apply plugin: "war"

bootWar {
    mainClassName = "io.github.huherto.MicroservApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
